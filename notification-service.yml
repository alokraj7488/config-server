spring:
  application:
    name: notification-service

server:
  port: 8086

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    hostname: 127.0.0.1
      
 rabbitmq:
   host: localhost
   port: 5672
   username: guest
   password: guest
